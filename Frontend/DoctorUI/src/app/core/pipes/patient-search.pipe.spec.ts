import { PatientSearchPipe } from './patient-search.pipe';

describe('PatientSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
