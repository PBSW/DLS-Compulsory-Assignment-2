import { PatientSeenPipe } from './patient-seen.pipe';

describe('PatientSeenPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientSeenPipe();
    expect(pipe).toBeTruthy();
  });
});
